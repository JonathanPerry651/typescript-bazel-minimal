load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "index",
    srcs = ["index.ts"],
    source_map = True,
    tsconfig = "//:tsconfig_build",
    deps = [],
    transpiler = "tsc",

)

filegroup(
    name = "assets",
    srcs = ["index.html", ":index"],
)

sh_binary(
    name = "server",
    srcs = ["server.sh"],
    data = [":assets"],
)
